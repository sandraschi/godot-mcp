extends Node2D
## Game Builder template — replace via generate_game_logic + write_scripts_to_project.

func _ready() -> void:
	print("Game Builder template loaded — run build_game to generate scripts.")
